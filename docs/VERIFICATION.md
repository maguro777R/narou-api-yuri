# 動作確認の記録

## ローカルで確認したこと（2026-09-18）

- Python の自動テスト 34 件が成功。
- JavaScript の構文チェックが成功。
- 架空データで一般向けと R18 の両ページを生成。
- 一般向けのジャンル・作品形式の切り替え、再読み込み時の条件復元。
- R18 ページの年齢確認前の非表示と、確認後の図表・形式切り替え。
- パソコン表示と幅 390 px の表示。横にはみ出さないことを確認。
- 取得から 15 日経った試験用ページで、集計と年齢確認ボタンが非表示になることを確認。
- 一般向けに R18 作品情報が混ざらないこと、休止時に R18 の古いページも置き換わること。

再現するコマンド:

```sh
python3 -m unittest discover -v
node --check web/app.js
python3 -m scripts.build --demo
```

自動テストでは架空の API 応答を使います。作品情報の欠落、対象外サイト・対象外条件の混入、重複、並び順、再試行、集計、表示時のエスケープ、期限を検証します。

## 実 API と GitHub Pages

実データの取得は、運用者のなろうユーザ登録と R18 利用の年齢条件の確認待ちです。架空データでの成功を実 API の動作確認とは扱いません。

公開・週次実行の確認結果は、実行後にこの欄へ記録します。
